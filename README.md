# claim.it-back

## Run

```bash
touch env.list
docker-compose up
```

## Env variables
* `MONGODB_URI`
* `MONGODB_DB_NAME`
* `ETH_NODE_URI`
* `ETH_NODE_WS_URI`
* `ETH_PRIVATE_KEY`
* `PUSHER_APP_ID`
* `PUSHER_APP_KEY`
* `PUSHER_APP_SECRET`
* `PUSHER_APP_CLUSTER`

## Routes

* `GET` `/health`