# claim.it-back

## Run

```bash
touch env.list
docker-compose up
```

## Env variables
* `MONGODB_URI`
* `MONGODB_DB_NAME`
* `ETH_NODE_URI`
* `ETH_NODE_WS_URI`
* `ETH_PRIVATE_KEY`

## Routes

* `GET` `/health`